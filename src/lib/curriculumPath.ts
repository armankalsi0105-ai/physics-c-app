/**
 * Single curriculum source path.
 * Runtime import: `@/data/curriculum.json` (copied from `data/curriculum.json`).
 * Run `npm run sync:curriculum` after editing `data/curriculum.json`.
 */
export const CURRICULUM_SOURCE = 'data/curriculum.json'
export const CURRICULUM_RUNTIME = 'src/data/curriculum.json'
